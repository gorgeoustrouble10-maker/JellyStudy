# 本地验证与调试清单（已一键启动后使用）

## 当前应已运行的服务

| 服务 | 地址 | 验证方式 |
|------|------|----------|
| 前端 | http://127.0.0.1:9945/ | 浏览器打开；刷新后保留页面可用 `#question`、`#evaluation` 等 |
| 健康检查 | 各服务 `GET /api/health` | 前端每 15s 探测 8081/8082/8083，侧栏与顶栏显示真实状态 |
| 知识点 API | http://127.0.0.1:8081/api/knowledge-points | 浏览器或 curl |
| 问答 API | http://127.0.0.1:8082/api/questions | 同上 |
| 评估 API | http://127.0.0.1:8083/api/evaluations/questions | 同上 |
| Nacos | http://127.0.0.1:8848/nacos | 用户 nacos / nacos |
| SkyWalking | http://127.0.0.1:8090/ | 操作业务后看 Topology |
| Swagger | http://127.0.0.1:8082/swagger-ui.html | 试调接口 |

## 建议手动走一遍（找 Bug）

1. **知识点**：新建 → 编辑 → 删除  
2. **问题**：选知识点 → 提问 → **点击卡片展开**（浏览量 +1）→ 看热门/常看榜 → 搜索关键字 → 删除后评估页应同步消失  
3. **回答**：对问题回答 → 点赞 → 评论  
4. **评估**：查看问题/回答评估；新建问题后等几秒看是否自动评估（Dubbo）  
5. **Redis**：`docker exec jellystudy-redis redis-cli -a jellystudy_redis ZREVRANGE jelly:hot:questions 0 5 WITHSCORES`  
6. **SkyWalking**：做完 2～4 后刷新 8090，看成功率是否 >0%

## 常见问题

| 现象 | 处理 |
|------|------|
| 前端 502 | 确认 8081/8082/8083 都在；重启 `scripts\start-java-services.ps1` |
| 评估一直 Mock | 检查 `local-secrets.bat` 里有效 `sk-` 密钥，重启 evaluate |
| SkyWalking 0% | 先访问几次 API，再等 1 分钟刷新 |
| Redis 连不上 | `docker compose -f docker-compose.core.yml up -d redis` |
| 浏览量不变 | 需在问题列表**展开**该问题（会调 `GET /questions/{id}`） |
| 删除问题后评估还在 | 重启 QA+Evaluate 后重删；评估页点**刷新**会自动清理历史孤立记录 |
| 评估页 ID 很长 | 正常 UUID，界面已显示短编号；鼠标悬停可看完整 ID |

## 再次一键启动

```bat
docker compose -f docker-compose.core.yml up -d
powershell -ExecutionPolicy Bypass -File scripts\start-java-services.ps1
cd frontend && npm run dev
```

## 实验报告与截图

- 正式稿：`实验报告-Redis与微服务.md`（第九节已插入全部截图）→ 导出 `32308117_吕宇轩.pdf`
- 截图步骤：`docs/截图操作指南.md` → PNG 放到 `docs/screenshots/01-…08-….png`
- IDE 少红点：`docs/IDE-Java-Lombok.md`，或双击打开 `JellyStudy.code-workspace`
