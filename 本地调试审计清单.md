# 本地全栈调试审计清单

**一键启动**（Docker Desktop 已开）：

```powershell
docker compose -f docker-compose.core.yml up -d
powershell -File scripts\import-nacos-config.ps1
powershell -File scripts\start-full-stack.ps1
cd frontend; npx vite --host 127.0.0.1 --port 9945
```

---

## 当前服务状态速查

| 组件 | 地址 | 验证命令 |
|------|------|----------|
| 前端 | http://127.0.0.1:9945/ | 浏览器打开，侧栏 4 服务绿点 |
| Gateway | http://127.0.0.1:8080/api/gateway/ping | 返回 `status: ok` |
| 知识点 | http://127.0.0.1:8081/api/health | UP |
| 问答 | http://127.0.0.1:8082/api/health | UP |
| 评估-1 | http://127.0.0.1:8083/api/evaluations/instance-info | instanceId + modelName |
| 评估-2 | http://127.0.0.1:8085/api/evaluations/instance-info | `evaluate-2` |
| Gateway LB | 多次请求 8080 instance-info | 8083/8085 交替 |
| Coach 配置 | http://127.0.0.1:8084/api/coach/config | dailyGoalCount 等 |
| Nacos | http://127.0.0.1:8848/nacos | nacos/nacos |
| SkyWalking | http://127.0.0.1:8090/ | 服务 Trace |

登录：`32308117` / `123456` 或 `demo` / `demo123`

---

## 第十二周功能审计步骤

### 1. Docker 双实例
```powershell
Invoke-RestMethod http://127.0.0.1:8083/api/evaluations/instance-info
Invoke-RestMethod http://127.0.0.1:8085/api/evaluations/instance-info
# 应看到不同 instanceId 和 serverPort
```

### 2. Gateway 负载均衡
```powershell
1..5 | % { (Invoke-RestMethod http://127.0.0.1:8080/api/evaluations/instance-info).serverPort }
# 应出现 8083 与 8085 交替
```

### 3. Nacos 配置热刷新
1. Nacos 控制台改 `jellystudy-coach-service.yaml` 中 `daily-goal-count: 5` → 发布
2. `Invoke-RestMethod http://127.0.0.1:8084/api/coach/config` → 应返回 5

### 4. JellyCoach 闭环
1. 登录 → 成长教练
2. 智能评估页提交回答 → 看 Coach 积分是否增加
3. 薄弱点「练习」→ AI 出题 → 提交

### 5. Dubbo + Nacos
- Nacos → 服务管理 → 服务列表：可见 knowledge/qa/evaluate/coach Provider

---

## 日志位置

| 服务 | 日志 |
|------|------|
| 各 Java 服务 | `logs/*.out.log` / `logs/*.err.log` |
| evaluate-2 | `logs/evaluate-2.out.log` |
| Gateway dual | `logs/gateway-dual.out.log` |

---

## 常见问题

| 现象 | 处理 |
|------|------|
| Docker 连不上 | 先开 Docker Desktop |
| 8082 500 | 等 60s 或看 `logs/qa.err.log`，点前端「重试」 |
| Gateway 8080 起不来 | 勿在 shell 里 `set SERVER_PORT=8085` 后启动 Gateway；用 `start-full-stack.ps1` |
| evaluate 启动失败 Ambiguous mapping | 已删重复 `InstanceInfoController`，需重新 `mvn package` |
| 首次启动慢 | knowledge/evaluate 可能需 2 分钟（SkyWalking Agent） |

---

## 自动化验证

```powershell
powershell -File scripts\verify-z12-demo.ps1
```
